target/
.m2/

.idea/
*.iml
.factorypath

.settings/
.classpath
.project

.DS_Store
bin
.flattened-pom.xml

node_modules/
**/frontend/generated/
**/frontend/index.html
package-lock.json
webpack.generated.js
tsconfig.json
types.d.ts
vite.generated.ts

/.m2/

# Claude
/.claude/worktrees/
CLAUDE.local.md
.claude/agent-memory

# DevContainer — local-only / per-developer (never committed)
.devcontainer/.plugin-marketplace/
.devcontainer/.env.local
.claude/settings.local.json
# Project MCP servers — machine/IDE-specific (e.g. the JetBrains "idea" bridge on
# localhost); enablement lives in the gitignored .claude/settings.local.json.
.mcp.json
.devcontainer/scripts/local-init.sh
.claude/reports
# >>> devcontainer-stack (managed) >>>
# scripts/perf-host.sh writes its runs here. Results are worth keeping, but somewhere durable
# rather than in the repository — each run is logs plus raw JSON.
perf-results/

# graphify: ignore the rebuildable cache,  graph.json / wiki.
graphify-out
.claude/skills/graphify
# <<< managed <<<
/.claude/settings.json.graphify-bak
